name: Keep Render App Awake

on:
  schedule:
    - cron: '*/6 * * * *'  # кожні 6 хвилин
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: curl -sSf https://exam-clinic-api.onrender.com || echo "Ping failed"
